const totalTime = 3000;
const totalHeight = 300;
const strokeWidth = 6;
const thumbRadius = strokeWidth;
const clearence = strokeWidth / 2;

export { totalTime, totalHeight, clearence, strokeWidth, thumbRadius };
